# BEO Importer (宴会活动导入器 — 已合并)

此项目已被 **`beo-analysis`** 项目合并。请使用 `projects/beo-analysis/`。

`beo-analysis` 包含 8 个脚本，覆盖 BEO 合同导入、多版本追踪、营收分析。

## 迁移指引

```bash
# 原来在 beo-importer 寻找的功能 → 已迁移到:
cd projects/beo-analysis/scripts/
ls _import_beo*.py
```
